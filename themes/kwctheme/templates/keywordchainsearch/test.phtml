<table>
   <th><?$this->transEsc('Browse Keywordchains')?></th>
<tr>
   <? if (!empty($this->resultList)):?>
   <? foreach ($this->resultList as $current):?>
       <?=$this->escapeHtml($current['value'])?> <br/>
   <? endforeach; ?> 
   <? endif; ?>
</tr>
</table>

