<? foreach (($formats = $this->driver->getFormats()) as $format): ?>
  <span class="iconlabel <?=$this->record($this->driver)->getFormatClass($format)?>"><?=$this->transEsc($format)?><?=($i++ < count($formats) - 1) ? " |" : ""?> </span>
<? endforeach; ?>
